@model IEnumerable<InsuranceManagement.Models.Agent>

@{
    ViewBag.Title = "Index";
}

<h2>Agents</h2>
<br />
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mail)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Phone)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mail)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id = item.AgentId }) |
            @Html.ActionLink("Reset Password", "ResetPassword", new { id = item.AgentId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.AgentId })
        </td>
    </tr>
}

</table>
